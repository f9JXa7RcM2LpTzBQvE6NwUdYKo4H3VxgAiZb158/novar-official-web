{
  "name": "novar-web",
  "private": true,
  "type": "module",
  "description": "NOVAR marketing site. Static — no build step. `type: module` tells Node the js/ files are ES modules (the browser already knows via <script type=\"module\">), which is what lets the unit tests import them.",
  "scripts": {
    "test": "node --test tests/"
  }
}
